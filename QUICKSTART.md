# ClipMate 快速入门指南

## 🚀 5分钟快速开始

### 1. 安装 ClipMate

```bash
# 克隆项目
cd /Users/coso/Documents/dev/ai/wordflowlab/clipmate

# 安装依赖
npm install

# 构建
npm run build

# (可选)全局安装
npm link
```

### 2. 安装必要工具

```bash
# macOS
brew install ffmpeg python3

# 安装 Python 依赖
pip3 install opencv-python numpy
```

### 3. 创建视频项目

```bash
# 方式1: 通过 CLI 创建
node dist/cli.js init my-video --ai claude

# 方式2: 在已有目录初始化
cd my-existing-project
node /path/to/clipmate/dist/cli.js init --here --ai claude
```

### 4. 导入视频

将你的视频文件放入 `videos/` 目录:

```bash
cp ~/Downloads/my-recording.mp4 videos/
```

### 5. 在 AI 编辑器中使用

在 Claude Code/Cursor 中打开项目,然后输入:

```
/import
```

AI 会自动:
- 分析视频信息
- 显示分辨率、时长、质量等
- 建议下一步操作

```
/detect
```

AI 会:
- 运行智能检测
- 展示 ABCDE 选择模式
- 让你选择检测预设(教学/会议/vlog/短视频)
- 生成详细的检测报告

```
/cut --auto
```

AI 会:
- 根据检测报告自动剪辑
- 删除静音片段
- 加速重复画面
- 生成剪辑后的视频

```
/export --preset youtube
```

最终导出适合 YouTube 的成品视频!

---

## 📋 完整工作流示例

### 场景: 剪辑30分钟的在线课程录制

**Step 1: 初始化项目**

```bash
node dist/cli.js init python-course --ai claude
cd python-course
```

**Step 2: 导入视频**

```bash
# 复制视频到项目
cp ~/Desktop/python-lesson-01.mp4 videos/
```

**Step 3: 在 Claude Code 中使用**

打开 Claude Code,输入:

```
/import
```

**AI 响应**:
```
✓ 视频导入成功!

📹 视频信息:
  文件名: python-lesson-01.mp4
  时长: 30分00秒
  分辨率: 1920x1080 (Full HD)
  文件大小: 450 MB

✅ 质量评估: 高质量视频
💡 建议: 运行 /detect 进行智能检测
```

**Step 4: 智能检测**

```
/detect
```

**AI 响应**:
```
✓ 视频分析完成

检测模式选择:

A. 🎓 教学演示模式 ⭐ 推荐
   - 删除讲解间隙的沉默
   - 加速重复的鼠标操作
   - 适合: 在线课程、软件演示

B. 📊 会议录制模式
C. 📹 Vlog 生活模式
D. ⚡ 短视频模式
E. 💭 自定义模式

请选择 A/B/C/D/E:
```

**用户选择**: A

**AI 继续**:
```
✓ 使用教学演示模式检测中...

╔═══════════════════════════════════════╗
║     视频检测报告                       ║
╚═══════════════════════════════════════╝

🔇 静音片段: 12 处 (共 3分15秒)
🔁 重复画面: 5 处 (共 1分30秒)

⏱️ 预计剪辑后:
  原始: 30分00秒
  新时长: 26分00秒
  节省: 4分00秒 (13.3%)

💡 建议: 运行 /cut --auto 自动剪辑
```

**Step 5: 自动剪辑**

```
/cut --auto
```

**AI 响应**:
```
✓ 正在剪辑视频...

[████████████░░] 80% 处理中...

✓ 剪辑完成!

✂️ 剪辑后视频:
  文件: clips/python-lesson-01-edited.mp4
  时长: 26分00秒 (节省 4分钟)
  大小: 390 MB

💡 下一步: 运行 /export --preset youtube
```

**Step 6: 导出成品**

```
/export --preset youtube
```

**AI 响应**:
```
✓ 导出完成!

📹 导出文件:
  路径: exports/video-youtube.mp4
  大小: 385 MB
  分辨率: 1920x1080 @ 30fps

🎉 视频处理完成!现在可以上传到 YouTube 了。
```

---

## 📁 项目目录结构说明

```
python-course/
├── .clipmate/              # 项目配置
│   └── config.json
│
├── .claude/                # Claude Code 配置
│   └── commands/           # Slash Commands
│       ├── import.md
│       ├── detect.md
│       ├── cut.md
│       ├── merge.md
│       └── export.md
│
├── scripts/                # 处理脚本
│   ├── bash/               # Bash 脚本
│   └── python/             # Python 处理脚本
│
├── templates/              # AI 模板
│   ├── commands/
│   └── option-templates/
│
├── videos/                 # 📹 原始视频 (你要放视频的地方)
│   └── python-lesson-01.mp4
│
├── clips/                  # ✂️ 剪辑片段和报告
│   ├── detect-report.json
│   └── python-lesson-01-edited.mp4
│
└── exports/                # 📦 导出成品
    └── video-youtube.mp4
```

---

## 💡 使用技巧

### 1. 批量处理多个视频

将所有视频放入 `videos/` 目录,然后:

```
/import
```

AI 会显示视频列表让你选择。

### 2. 调整检测参数

如果自动检测不够准确:

```
/detect --custom
```

AI 会询问你:
- 静音阈值(几秒算静音)
- 音量阈值(多少 dB 算静音)
- 重复相似度

### 3. 交互式确认剪辑

首次使用不确定效果?

```
/cut --interactive
```

AI 会逐个片段询问你是否删除/保留/加速。

### 4. 预览剪辑计划

想先看看会怎么剪辑?

```
/cut --preview
```

AI 会显示完整的剪辑计划,但不实际执行。

---

## ❓ 常见问题

### Q1: 提示 "FFmpeg 未安装"

**解决**:
```bash
# macOS
brew install ffmpeg

# Ubuntu
sudo apt-get install ffmpeg
```

### Q2: Python 脚本执行失败

**解决**:
```bash
# 安装 Python 依赖
pip3 install opencv-python numpy pydub
```

### Q3: 检测时间过长

**原因**: 视频较长(>60分钟)时,检测需要5-10分钟

**解决**:
- 对短片段先测试
- 或使用 `--quick` 快速模式(精度略低)

### Q4: 剪辑后音画不同步

**解决**:
```
/cut --auto --resync
```

使用重新同步选项。

---

## 🎯 不同场景最佳实践

### 场景1: 在线课程/教学视频

```
预设: teaching (教学模式)
流程: /import → /detect → /cut --auto → /export --preset youtube
特点: 删除停顿,加速重复操作,保持讲解完整
```

### 场景2: 会议录制

```
预设: meeting (会议模式)
流程: /import → /detect --preset meeting → /cut --interactive → /export
特点: 保留自然节奏,只删除长时间静音
```

### 场景3: Vlog/生活记录

```
预设: vlog (生活模式)
流程: /import → /detect --preset vlog → /cut → /export --preset bilibili
特点: 轻度优化,保持自然,添加场景转场
```

### 场景4: 短视频素材

```
预设: short (短视频模式)
流程: /import → /detect --preset short → /cut --auto → /export --preset douyin
特点: 严格删除冗余,快节奏,竖屏输出
```

---

## 🔧 高级功能

### 自定义检测阈值

编辑 `templates/option-templates/detect-presets.yaml` 添加自己的预设。

### 批量处理脚本

创建 bash 脚本自动化处理:

```bash
#!/bin/bash
for video in videos/*.mp4; do
  node /path/to/clipmate/dist/cli.js import "$video"
  # ... 后续处理
done
```

---

## 📚 更多资源

- [完整文档](README.md)
- [GitHub 仓库](https://github.com/wordflowlab/clipmate)
- [问题反馈](https://github.com/wordflowlab/clipmate/issues)

---

**开始你的第一个视频剪辑项目吧! 🎬**
